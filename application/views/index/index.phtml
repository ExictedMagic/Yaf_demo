<h2><a href="/index/add">添加</a></h2>
<?php
$html = "";
if ($data['userList']) {
    $html .= "<ul>";
    foreach ($data['userList'] as $line) {
        $html .= "<li>{$line['username']} <a href=''>[编辑]</a> <a href=''>[删除]</a></li>";
    }
    $html .= "</ul>";
}
echo $html;